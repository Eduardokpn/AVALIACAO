<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MysticPartyTracker.View.ResponseView"
             Title="ResponseView">


    <ScrollView>
        <VerticalStackLayout>
            <CollectionView ItemsSource="{Binding Result}" EmptyView="Deu bosta">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <VerticalStackLayout>
                        <Label
                            Text="{Binding Index}"/>

                        <Label
                            Text="{Binding Name}"
                            />

                        <Label
                            Text="{Binding Level}"
                            />

                        <Label
                            Text="{Binding Url}"
                            />
                        </VerticalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
        
        
        
    </ScrollView>            
    
    
</ContentPage>